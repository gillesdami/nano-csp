
dom.addEventListener(e, f);

take(l)

async function* asyncChannel(emiter) {
    const buffer = [];

    emiter((emit) => {
        buffer.push(emit);
        
    });

    do {
        buffer.unshift
    } while(yield eventPromise)
}